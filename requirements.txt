# Streamlit核心包（指定具体版本比 >= 更稳定，避免线上自动升级到有Breaking Change的版本）
streamlit==1.36.0

# 数据处理包
pandas==2.1.0
openpyxl==3.1.2       # 读取.xlsx文件
python-dotenv==1.0.0  # 环境变量管理

# 数据格式支持
pyarrow==14.0.0       # Parquet/CSV高性能读取
# csvkit==1.0.7      # 注释掉，pandas通常已足够，除非有特殊命令行需求

# 图像处理（Streamlit通常依赖Pillow处理图片，若用到图片建议加上）
Pillow==10.2.0
